{
  "name": "@annotedly/worker-ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Annotedly ingestion worker — polls source feeds, normalizes raw entries into canonical items",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
